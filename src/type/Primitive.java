package type;

public class Primitive {
    //프리미타입 = 기본 타입
    //기본값이 null 이 아닌 타입
    //해당 변수가 선언시 JVM 의 Runtime Data Area에 Stack memory에 값과 함께 저장이된다.

    char primitiveChar;
    int primitiveInt;
    long primitiveLong;
    float primitiveFloat;
    double primitiveDouble;
    boolean primitiveBoolean;
    byte primitiveByte;
    short primitiveShort;

}
